import merge from 'lodash/merge';

const TracksReducer = (state = {}, action) => {
  Object.freeze(state);
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state;
  }
};

export default TracksReducer;
